# Static site (future GitHub Pages)

This directory is reserved for a **future** static frontend (e.g. multiscale viewer for JSON exports).

- **Status:** placeholder only — no SPA or build step yet.
- **Planned use:** publish `site/` as GitHub Pages (or `docs/` build output) once a minimal demo exists.

See the root **README.md** section *Planned GitHub Pages deployment*.
